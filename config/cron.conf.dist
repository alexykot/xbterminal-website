# Check wallet every 30 minutes
*/30 * * * * root cd /repo_root && . venv/bin/activate && python xbterminal/manage.py check_wallet
# Sync with cryptopay every 30 minutes
*/30 * * * * root cd /repo_root && . venv/bin/activate && python xbterminal/manage.py cryptopay_sync
